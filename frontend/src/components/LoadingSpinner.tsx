const LoadingSpinner = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <span className="loading fixed loading-spinner loading-lg"></span>
    </div>
  );
};

export default LoadingSpinner;
